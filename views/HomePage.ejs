<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bus booking website</title>
  <style>
    /* Base styles */
    body {
      font-family: sans-serif;
      margin: 0;
      padding: 20px;
      background-color: #f0f0f0; /* Light background color */
    }

    h1, h2 {
      margin: 0;
      padding: 0;
    }

    /* Header styles */
    header {
      text-align: center;
      padding-bottom: 20px;
      border-bottom: 1px solid #ddd; /* Add a subtle border */
    }

    h1 {
      font-size: 2em;
      margin-bottom: 10px;
    }

    p {
      font-size: 0.9em;
      color: #666; /* Subdued text color */
    }

    /* Main content styles */
    main {
      display: flex;
      justify-content: center; /* Center content horizontally */
      align-items: center; /* Center content vertically */
      min-height: calc(100vh - 120px); /* Set minimum height for viewport coverage (excluding header) */
    }

    .user-options {
      background-color: #fff; /* White background for user options */
      padding: 20px;
      border-radius: 5px; /* Rounded corners */
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1); /* Subtle shadow */
    }

    h2 {
      margin-bottom: 15px;
    }

    .options {
      display: flex;
      justify-content: space-around; /* Distribute links evenly */
    }

    a {
      text-decoration: none; /* Remove underline from links */
      color: #333; /* Text color for links */
      padding: 10px 20px;
      border: 1px solid #ddd; /* Border for links */
      border-radius: 5px;
      transition: all 0.2s ease-in-out; /* Smooth hover effect */
    }

    a:hover {
      background-color: #eee; /* Light background on hover */
    }

    /* Responsive styles */
    @media screen and (max-width: 768px) {
      h1 {
        font-size: 1.5em;
      }

      .user-options {
        width: 100%; /* Make user options section full width on small screens */
      }
    }
  </style>
</head>
<body>
    <header>
        <h1>Welcome to Rentify</h1>
        <p>Rent or buy properties, as valuable as it can get!</p>
    </header>

    <main>
        <section class="user-options">
            <h2>Let's Begin !</h2>
            <div class="options">
                <a href="/login">Login</a>
                <a href="/signup">Signup</a>
            </div>
        </section>
    </main>
</body>
</html>
