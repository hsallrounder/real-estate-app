<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
  <style>
    /* Base styles */
    body {
      font-family: sans-serif;
      margin: 0;
      padding: 20px;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }

    header, main {
      flex: 1; /* Make header and main fill available space */
    }

    h1 {
      text-align: center;
      margin-bottom: 10px;
    }

    p {
      text-align: center;
      margin-top: 0;
    }

    .err_msg {
      text-align: left;
      color: red;
      margin-bottom: 0;
      font-size: 12px;
      padding: 0;
    }
    
    /* Centered form with decent width */
    form {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 10px; /* Add spacing between form elements */
      width: 400px; /* Set form width */
      margin: 0 auto; /* Center form horizontally */
    }

    label {
      display: block;
      margin-bottom: 5px;
    }

    /* Consistent input width (adjust as needed) */
    input, select {
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 5px;
      width: 75%; /* Adjust input width (e.g., 80% or 90%) */
    }

    .signup-btn {
      background-color: #4CAF50;
      color: white;
      padding: 10px 20px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }

    .signup-btn:hover {
      background-color: #3e8e41;
    }

    /* Login page specific styles (optional) */
    .signup-btn {  /* Adjust button color if desired */
      background-color: #2196F3; /* Blue theme */
    }

    h1 {  /* Adjust heading color if desired */
      color: #2196F3; /* Blue theme */
    }

    .signup-link {
      text-align: center;
      margin-top: 20px;
    }

    /* Responsive adjustments (optional) */
    @media screen and (max-width: 768px) {
      body {
        padding: 10px;
      }

      h1 {
        font-size: 1.5rem;
      }

      form {
        width: calc(100% - 20px); /* Set form width to 100% minus padding */
      }
    }
  </style>
</head>
<body>
    <header>
        <h1>LOGIN</h1>
        <p>Log into your account to buy or upload properties.</p>
    </header>

    <main>
        <form action="/login" method="post">
            <label for="email">Email:</label>
            <input type="email" id="email" name="email" value="<%= email %>" required>

            <label for="password">Password:</label>
            <input type="password" id="password" name="password" value="<%= password %>" required>

            <p class="err_msg">
              <%= err %>
            </p>

            <button type="submit" class="signup-btn">Login</button>
        </form>
        <p class="signup-link">New user? <a href="/signup">Register here</a>.</p>
    </main>
</body>
</html>
