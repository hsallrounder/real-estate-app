<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Property</title>
  <style>
    body {
      font-family: sans-serif;
      margin: 0;
      padding: 20px;
    }

    h2 {
      text-align: center;
      margin-bottom: 20px;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 10px; /* Add spacing between form elements */
      width: 500px; /* Set form width */
      margin: 0 auto; /* Center form horizontally */
      padding: 20px; /* Add padding for better spacing */
      border: 1px solid #ddd; /* Add a subtle border */
      border-radius: 5px; /* Rounded corners */
    }

    label {
      display: block;
      margin-bottom: 5px;
    }

    input[type="text"],
    input[type="number"] {
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 5px;
      width: 100%;
    }

    .amenities {
      display: flex;
      flex-wrap: wrap; /* Allow checkboxes to wrap on small screens */
      gap: 10px; /* Add spacing between checkboxes */
    }

    .amenities label {
      display: inline-block;
      margin-right: 10px;
    }

    button {
      background-color: #4CAF50; /* Green button */
      color: white;
      padding: 10px 20px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      margin-top: 10px; /* Add spacing above button */
    }

    button:hover {
      background-color: #3e8e41; /* Darker green on hover */
    }
  </style>
</head>
<body>
  <h2>Edit Property Details</h2>
  <form action="/editProperties" method="post">
    <label for="areaName">Area Name:</label>
    <input type="text" id="areaName" name="areaName" value="<%= areaName %>" required>

    <label for="plotSize">Plot Size (sq ft):</label>
    <input type="number" id="plotSize" name="plotSize" min="0" value="<%= plotSize %>" required>

    <label for="bedrooms">Number of Bedrooms:</label>
    <input type="number" id="bedrooms" name="bedrooms" min="0" value="<%= bedrooms %>" required>

    <label for="bathrooms">Number of Bathrooms:</label>
    <input type="number" id="bathrooms" name="bathrooms" min="0" value="<%= bathrooms %>" required>

    <h3>Amenities (Select all that apply):</h3>
    <div class="amenities">
      <label for="hotel">Hotel Nearby:</label>
      <input type="checkbox" id="hotel" name="amenities" value="hotel" checked="<%= checkHotel %>">

      <label for="college">College Nearby:</label>
      <input type="checkbox" id="college" name="amenities" value="college" checked="<%= checkCollege %>">

      <label for="hospital">Hospital Nearby:</label>
      <input type="checkbox" id="hospital" name="amenities" value="hospital"  checked="<%= checkHospital %>">

      </div>

    <button type="submit">Update Property</button>
  </form>
</body>
</html>
